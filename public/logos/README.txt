Put the official Zoho product logos here so the Connections screen shows them:

  public/logos/zoho-crm.svg       (or .png)
  public/logos/zoho-creator.svg   (or .png)

Download them from Zoho's brand / press kit. If you only have PNGs, save them
with a .png extension and change the extensions in src/business/ZohoLogos.jsx
(the LOGO_SRC map at the top).

Until these files exist, the app shows a clean brand-coloured fallback mark.
